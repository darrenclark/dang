import Std/Builtins.inspect

let pick = fn(which_one, a, b) {
  if which_one {
    let x = 1
    fn () { a }
  } else {
    let x = 1
    let y = 1
    fn () { a + b }
  }
}

inspect(pick(false, 1, 2)())
