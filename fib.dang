fn fib(n) {
  /*if n <= 1 {
    return n;
  } else {
    return fib(n - 1) + fib(n - 2);
  }*/

  if n {
  } else { // n == 0
    return n;
  }

  if n - 1 {
  } else { // n == 1
    return n;
  }

  return fib(n - 1) + fib(n - 2);
}

return fib(8);
